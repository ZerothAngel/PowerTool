config-version: 4

player-token: '%p'

x-token: '%x'

y-token: '%y'

y-air-token: '%Y'

z-token: '%z'

options:
- name: default
  limit: -1
- name: group.guest
  limit: 5

powertools:
  sulphur:
    left-click: version
    right-click: plugins

verbose: true

omit-first-slash: true

debug: false
